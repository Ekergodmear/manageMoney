# Spec 002 — Examples

```typescript
const ok: Result<number, string> = { ok: true, value: 42 };
const err: Result<number, string> = { ok: false, error: 'invalid' };
```

StrategyInput example — see `tests/fixtures/fixed-profit-x20-5-rounds.json`.
