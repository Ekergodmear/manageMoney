# Spec 007 — Requirements

- [ ] `docs/Mathematical-Proof.md` approved before code
- [ ] `optimize(input): Result<OptimizationResult, OptimizationError>`
- [ ] Uses solver — does not import simulation
- [ ] See KNOWN_LIMITATIONS — no Monte Carlo
