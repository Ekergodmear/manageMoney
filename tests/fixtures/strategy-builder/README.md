# StrategyBuilder Golden Fixtures

Byte-stable `Round[]` → `Strategy` mapping for builder-only tests.

**Do not** include solver requests — solver has its own golden suite.

See `docs/RELEASE-RULES.md` Rule 1 — manual updates only.
